{% include '_context.md.j2' %}
## Task: Transitions pass

Examine how this chapter connects to its neighbors:

- How should this chapter open, given how the previous one ended (tonally, in time, in place)? Avoid a jarring or redundant restart.
- What should the last lines/paragraph set up so the next chapter has something to land on (a question, a tension, an image to echo or subvert)?
- Is there a clean throughline (an object, image, line of dialogue, or unresolved question) that could carry across the boundary?
{% if extra %}
Author's specific focus for this pass: {{ extra }}
{% endif %}
Propose concrete opening and closing beats/lines, not general transition theory.
